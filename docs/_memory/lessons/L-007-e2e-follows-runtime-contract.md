# L-007 — E2E Must Follow the Current Runtime Contract

End-to-end coverage must exercise the current game in `rpg-maker/The Dryland Drowned/` through its documented local server and native engine lifecycle. Use the same entry and plugins players use; retain the read-only QA API. The earlier HTML execution contract is historical. Browser-free unit tests prove only their assigned domain invariants.

When the public browser QA contract changes, update the snapshot/error assertions and complete browser journeys in the same task that finishes the surface. Local offline behavior, seed reproducibility, and normal player controls remain part of every relevant E2E gate.
