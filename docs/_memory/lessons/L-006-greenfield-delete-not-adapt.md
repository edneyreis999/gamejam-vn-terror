# L-006 — Name Replacements and Delete Targets

When a spec replaces an old path, list the obsolete behavior, state, copy, tests, and documentation that must be removed. Do not preserve a compatibility branch unless the user-facing contract requires it. For incremental prototype changes, migrate the existing path directly and identify any obsolete linear-progression logic.
