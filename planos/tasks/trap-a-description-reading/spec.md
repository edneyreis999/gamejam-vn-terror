# Pool A description reading

## Approved scope

On 2026-09-22 the user accepted the proposed reading improvement and narrowed implementation to A1–A8 descriptions. This authorizes implementation, not final human acceptance. Preserve the completed [prose integration](../revised-trap-prose-integration/spec.md) as historical evidence.

Replace mechanical two-line splits with complete paragraphs in the existing four-row native message window. Target two acknowledgements per description: setting, then threat plus question on a new line. Preserve every word, the source order, the player-controlled pace and separate choice confirmation. MessageCore word wrap is already active; no plugin settings/code changes are required. If four rows fit, do not enlarge the window just to demonstrate six rows.

## Boundaries

Only description 101/401 bodies in Map007–014, event 1, page 1, reading IDs encounter.A1.01 through encounter.A8.01. Preserve all surrounding commands, reread/reading IDs, choices, outcomes, deaths, audio/art, global fonts, window geometry and checkpoints. Pool B remains unchanged. Existing serialized conversations can contain historical event lists; no migration is promised.

## Verification contract

Static verification compares exact catalogue wording and full native objects against the previous integration plus this explicit transformation. Browser verification starts a fresh isolated campaign through public input and inspects both pages of all eight A descriptions at 1280×720. Check no clipping/overflow, question presence, native controls and choice boundary; reread one encounter and reopen/Continue one self-produced checkpoint. Complete routes as needed to encounter all A IDs, using supported approaches for navigation. No injected state or external saves. Inspect screenshots, not only geometry. Human reading comfort remains pending.

G006 risk selection: one grouped native journey covers the changed description format; no success/death variant matrix because those command bodies remain byte-for-byte structurally equal to their verified baseline. No engine/plugin/save-schema mutation. Extra resolution/reduced-motion/input-device matrices omitted for unchanged geometry and controls; reopen if layout/input defects emerge. Gamepad/native zoom excluded under G005/G003. Close isolated QA resources; preserve the user's manual browser/server.

## Demonstrable moment

A1 threat and question together in the lower window; compare two description pages against the previous six. Retain a selected real screenshot after verification for review/devlog. No publication or commit.
