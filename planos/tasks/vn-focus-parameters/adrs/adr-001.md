# ADR001 — One project style for dialogue focus

Accepted through the user's explicit implementation instruction, 2026-09-11. Implement the five fields from the preceding proposal with existing defaults and no scene overrides. A positive numeric darkness control replaces raw tone arrays. A separate speaker multiplier defaults to100. Preserve native entry bases and source-derived reconstruction.

The initial command-shape migration advances native layout revision. Purely cosmetic parameter changes afterward use the current project configuration when a structurally compatible save resumes; they do not change campaign facts. Parameter fingerprints belong to QA evidence, not unconditional save invalidation. Preserve vendor plugins and all PNGs. Remaining interface/memorial customization and participant expansion are deferred known issues, not this delivery.


## Final acceptance — 2026-09-11

The user explicitly approved the completed development after reviewing the result and requested the specs be brought up to date. The approval covers the final combination of native bust authorship,12 positional helpers, reconstruction from that authorship and the five global Plugin Manager controls. This acceptance retains the earlier instruction to preserve PNGs for the user's later normalization. Participant expansion and the remaining editor-maintenance opportunities stay deferred in their known issues. It does not retroactively turn historical failed or unavailable sensors into passing evidence. The current delivery and acceptance record are owned by [spec.md](../spec.md) and [verification.md](../verification.md); later behavior changes need a new increment.
